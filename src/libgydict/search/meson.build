search_headers = [
  'gy-search-bar.h'
]

search_sources = [
  'gy-search-bar.c'
]

libgydict_public_headers += files(search_headers)
libgydict_public_sources += files(search_sources)

install_headers(search_headers, install_dir: join_paths(libgydict_header_dir, 'search'))
