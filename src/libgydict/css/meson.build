css_headers = [
  'gy-css-provider.h'
]

css_sources = [
  'gy-css-provider.c'
]

libgydict_public_headers += files(css_headers)
libgydict_public_sources += files(css_sources)

install_headers(css_headers, install_dir: join_paths(libgydict_header_dir, 'css'))
